@model HospitalMVC.Models.Hospital

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetHospitalList</title>
</head>
<body>
    <div>
        @foreach (var d in @Model.HospName)
    </div>
</body>
</html>
