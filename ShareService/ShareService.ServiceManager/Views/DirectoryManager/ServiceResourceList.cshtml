@model IEnumerable<ResoureManager.Content.Models.GISResource>

<div style="width:100%; height:100%;">
    <style>
        th, td {
            text-align: center;
        }
    </style>
    <script>
        var selectedDirectory = '@(ViewBag.DirectoryCode)';
            $(document).ready(function () {
                $('#resourceWindow').jqxWindow({
                    width: '400px',
                    height: '340px',
                    resizable: true,
                    okButton: $('#okButton'),
                    initContent: function () {
                        $('#okButton').jqxButton({ width: '80px', disabled: false });
                    }
                });
            });

            function createGISResource() {
                $('#loader').show();
                $.ajax({
                    url: '../GISResourceManager/Create?directoryCode=' + selectedDirectory,
                    success: function (data) {
                        $('#resourceWindow #titleContainer').html('添加GIS资源');
                        $('#resourceWindow #customWindowContent').empty().html(data);
                        $('#resourceWindow').jqxWindow('open');
                    },
                    complete:function()
                    {
                        $('#loader').hide();
                    }
                });
            }

            function editGISResource(resourceCode) {
                $('#loader').show();
                $.ajax({
                    url: '../GISResourceManager/Edit?resourceCode=' + resourceCode,
                    success: function (data) {
                        $('#resourceWindow #titleContainer').html('编辑GIS资源');
                        $('#resourceWindow #customWindowContent').empty().html(data);
                        $('#resourceWindow').jqxWindow('open');
                    },
                    complete: function () {
                        $('#loader').hide();
                    }
                });
            }

            function viewGISResource(resourceCode) {
                $('#loader').show();
                $.ajax({
                    url: '../GISResourceManager/Detail?resourceCode=' + resourceCode,
                    success: function (data) {
                        $('#resourceWindow #titleContainer').html('查看GIS资源');
                        $('#resourceWindow #customWindowContent').empty().html(data);
                        $('#resourceWindow').jqxWindow('open');
                    },
                    complete: function () {
                        $('#loader').hide();
                    }
                });
            }

            function deleteGISResource(resourceCode)
            {
                if (confirm('您确定删除该资源吗？')) {
                    $('#loader').show();
                    $.ajax({
                        url: '../GISResourceManager/Delete?resourceCode=' + resourceCode,
                        success: function (result) {
                            if (result.State) {
                                showServiceList(selectedDirectory);
                            }
                            else {
                                alert("删除该资源时发生错误，删除失败！");
                            }
                        },
                        error:function(msg)
                        {
                            alert("与服务器通讯发生异常，请检查网络状况！");
                        },
                        complete: function () {
                            $('#loader').hide();
                        }
                    });
                }
            }
    </script>
    <div style="padding:5px; text-align:right;">
        <input type="button" value="添加资源" class="btn btn-default"  onclick="createGISResource();" style="font-size:12px;float:left;"/>
        <input type="button" value="刷新列表" class="btn btn-default" onclick="showServiceList('@ViewBag.DirectoryCode')"  style="font-size:12px;"/>
    </div>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SourcePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VisitAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                操作
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SourcePath)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VisitAddress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a onclick="editGISResource('@item.Code')" style="cursor:pointer;">编辑</a>
                    <a onclick="viewGISResource('@item.Code')" style="cursor:pointer;">详细信息</a>
                    <a onclick="deleteGISResource('@item.Code')" style="cursor:pointer;">删除</a>
                </td>
            </tr>
        }

    </table>
</div>

